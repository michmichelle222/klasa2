const iks = document.querySelectorAll("#iks");
const wynik = document.querySelectorAll("#wynik");
const btn = document.querySelectorAll("button");

btn.addEventListener("click", function() {

    let p1 = Math.floor(Math.iks );



})